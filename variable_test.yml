---
 - name: Variable Test
   hosts: all
   become: yes
   vars:
     target_service: httpd
     target_state: latest
   tasks:
     - name: Ensure target service is installed
       yum:
         name: "{{ target_service }}"
         state: "{{ target_state }}"
         
