---
- name: Remove EPEL Repository
  hosts: all
  become: yes
  tasks:
    - name: Remove repository (and clean up left-over metadata)
      yum_repository:
        name: rhel-7-server-extras-rpms/x86_64
        state: absent
     # notify: yum-clean-metadata
